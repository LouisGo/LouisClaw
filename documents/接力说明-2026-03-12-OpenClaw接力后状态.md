# 接力说明（2026-03-12：OpenClaw 已在本机接好）

> 生成时间：2026-03-12
> 用途：给下一次接力的人快速说明：**代码侧已通过 Git 同步，OpenClaw 本机侧已配置完成，但项目 `.env` 还未补，所以流水线目前是“空跑可用”状态。**

---

## 1）当前结论（先看这个）

### 已完成

1. **代码侧能力已对齐并可通过 Git 同步**
   - 标准 task id 已稳定：
     - `pull_markdown_sources`
     - `status_overview`
     - `process_inbox`
     - `build_digest`
     - `daily_pipeline`
     - `export_siyuan`
   - 标准 schedule id 已稳定：
     - `hourly_pull_markdown_sources`
     - `hourly_process_inbox`
     - `daily_pipeline_evening`
   - `status` 已补最近 `pull/process/daily` 运行摘要与 schedule 状态。

2. **OpenClaw 本机侧已接力完成**
   - workspace 已绑定到当前仓库路径。
   - 默认模型已设为 `openai/gpt-5.4`。
   - `models.mode = merge` 已生效。
   - `openai` provider 已配置并通过 `openclaw config validate`。
   - 三条标准 cron 已安装并启用。
   - Web 侧已做过基础调试，能读仓库、能执行标准任务、能读到 schedule 状态。

3. **当前真正卡点不在 OpenClaw，而在项目 `.env`**
   - 项目根目录当前**没有 `.env`**。
   - 当前运行时**没有 `MARKDOWN_PULL_SOURCES`**。
   - 因此 `pull_markdown_sources` 虽然能运行，但没有任何 source 可拉取，属于“空跑成功”。

### 当前未完成

1. **项目本地 `.env` 还没补**
   - 这意味着 markdown pull 还没有真实输入源，也没有 mock 输入源配置。

2. **真实输入源还没接入**
   - 还停留在“框架和 OpenClaw 已接好”的阶段。

3. **尚未产出实际 item / digest**
   - 当前 `npm run status` 仍是空状态：`Items total = 0`、`Digest exists = no`。

---

## 2）代码侧（可通过 Git 同步）

以下内容会随仓库同步，新机器 `git pull` 后即可获得。

### 当前代码侧能力

1. **输入边界与处理边界仍然清晰**
   - `data/landing/`：外部输入落点
   - `data/inbox/`：内部标准处理队列
   - `process_inbox`：统一执行 `landing -> inbox -> process`

2. **markdown pull 仍保持最小 v1 策略**
   - 首次运行只 seed state，不导入历史
   - 后续仅导入 append 新增后缀
   - rewrite 覆写仅更新 state，不导入

3. **可观测性已前进一小步**
   - `npm run status` 会显示：
     - 最近 `pull_markdown_sources`
     - 最近 `process_inbox`
     - 最近 `daily_pipeline`
     - 三条标准调度是否 installed / enabled

4. **mock 契约文件仍在仓库里**
   - `mock-sources/markdown/inbox.md`
   - `mock-sources/markdown/quick-notes.md`
   - `mock-sources/markdown/reading-notes.md`

### 当前推荐执行顺序（代码侧）

1. `pull_markdown_sources`
2. `process_inbox`
3. `daily_pipeline`（低频）

---

## 3）OpenClaw 侧（不能通过 Git 同步，这部分最重要）

以下内容是**本机状态**，不会随 Git 同步；换机器后需要重新配置。

### 当前这台机器上的已确认状态

1. **workspace 绑定**
   - `agents.defaults.workspace = /Users/louistation/MySpace/Life/LouisClaw`

2. **模型与 provider**
   - 默认模型：`openai/gpt-5.4`
   - alias：`medium -> openai/gpt-5.4`
   - `models.mode = merge`
   - 已配置 `openai` provider（OpenAI-compatible）
   - provider 校验已通过：`openclaw config validate`

3. **工具权限与 gateway**
   - `tools.profile = coding`
   - `gateway.mode = local`
   - gateway 已重启并可正常工作

4. **已安装 cron（本机）**
   - `hourly_pull_markdown_sources`：`0 * * * *`（Asia/Shanghai）
   - `hourly_process_inbox`：`5 * * * *`（Asia/Shanghai）
   - `daily_pipeline_evening`：`10 21 * * *`（Asia/Shanghai）
   - 当前状态：三条均 `installed = yes`、`enabled = yes`

5. **Web 调试结果**
   - 新会话里可以完成只读仓库检查
   - 可以执行 `npm run task -- run status_overview`
   - 可以执行 `npm run schedule -- list`
   - 可以执行 `pull_markdown_sources` / `process_inbox`
   - 当前之所以没有产出，是因为项目 `.env` 为空，不是 OpenClaw 挂了

### 特别注意

1. `~/.openclaw/` **不会随 Git 同步**
   - 包括：
     - `openclaw.json`
     - gateway token
     - cron jobs state
     - 本机 auth / runtime 信息

2. **当前本机 OpenClaw 配置里已经有 provider 凭据**
   - 这只存在于本机，不在 Git 里。
   - 下次接力时不要以为 Git 同步后就会自动有这部分配置。

3. **如果之后要做安全收口**
   - 建议把本机 `openclaw.json` 里的 provider `apiKey` 改回 `${OPENAI_API_KEY}` 这类 env 引用。
   - 再把真实 key 放到本机 shell / launch env，而不是明文留在配置文件里。

---

## 4）本次接力的真实进度

### 已完成的任务

1. **复核项目实现没有明显背离设计初衷**
   - 标准任务层、调度层、landing/inbox 边界、SiYuan 增量导出边界都还在。

2. **补了 `status` 可观测性**
   - 现在可以直接从 `npm run status` 看到最近任务运行摘要与 schedule 状态。

3. **完成本机 OpenClaw 接力**
   - merge mode 已开启
   - provider 已接好
   - gateway 已重启
   - cron 已安装
   - Web 会话已调通

### 当前停下来的位置

1. **OpenClaw 层已经就绪**
   - 继续折腾 OpenClaw 的优先级已经不高。

2. **项目层还缺 `.env`**
   - 这是下一步的主线。

3. **还没进入真实数据闭环验证**
   - 还没有用 mock / real markdown sources 触发出真实 item。

---

## 5）下一步建议（按优先级排序）

### P1：先补项目根目录 `.env`

最小可用建议：

```bash
OPENAI_API_KEY=你的平台API_KEY
OPENAI_MODEL=gpt-5.4
MARKDOWN_PULL_SOURCES=[{"path":"./mock-sources/markdown/inbox.md","source":"markdown_pull","device":"local","title":"Mock Inbox"},{"path":"./mock-sources/markdown/quick-notes.md","source":"markdown_pull","device":"local","title":"Mock Quick Notes"},{"path":"./mock-sources/markdown/reading-notes.md","source":"markdown_pull","device":"local","title":"Mock Reading Notes"}]
```

目的不是长期使用 mock，而是先确认“项目 env -> pull -> process -> status”闭环真能跑通。

### P2：补一次最小闭环验证

在项目目录执行：

```bash
npm run task -- run pull_markdown_sources
npm run task -- run process_inbox
npm run status
```

预期：

- `pull_markdown_sources` 至少能看到 `seeded` 或 `skipped`
- `status` 不再是“完全没有 source 配置”的空跑状态

### P3：把 mock 源替换成真实 markdown 源

建议顺序：

1. 先接 1~2 个真实文件
2. 优先选择 append-only、路径稳定的文件
3. 例如：`inbox.md`、`速记.md`

### P4：再做一次 Web 调试

推荐消息：

1. `请在当前工作区直接运行 npm run task -- run pull_markdown_sources，并用简洁中文总结结果。`
2. `请在当前工作区直接运行 npm run task -- run process_inbox，并用简洁中文总结结果。`
3. `请在当前工作区直接运行 npm run status，并告诉我最近任务状态是否正常。`

---

## 6）边界说明（防止误操作）

- Git 只同步项目代码与文档，不同步 `~/.openclaw/`。
- 不要把项目 `.env` 提交进仓库。
- 当前 OpenClaw 已经可用，接下来的主线不是继续改 gateway，而是补项目 `.env` 与真实 source。
- OpenClaw Dashboard 调试请优先使用**新会话**，不要依赖旧会话上下文。
- 如果 Web 不响应，优先检查：
  - `openclaw logs --plain`
  - `openclaw models status --plain`
  - `openclaw cron list --all --json`
  - `npm run status`
